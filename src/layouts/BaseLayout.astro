---
import { ClientRouter } from 'astro:transitions';

// Local fonts - latin + latin-ext only (eliminates external DNS lookup)
// Latin includes æ, ø, å (U+0000-00FF), latin-ext for additional European characters
import '@fontsource/inter/latin-400.css';
import '@fontsource/inter/latin-ext-400.css';
import '@fontsource/inter/latin-500.css';
import '@fontsource/inter/latin-ext-500.css';
import '@fontsource/inter/latin-600.css';
import '@fontsource/inter/latin-ext-600.css';
import '@fontsource/inter/latin-700.css';
import '@fontsource/inter/latin-ext-700.css';

interface Props {
  title: string;
  description: string;
  canonical?: string;
  ogImage?: string;
}

const {
  title,
  description,
  canonical = 'https://efffekt.no/',
  ogImage = 'https://efffekt.no/assets/og-image.png?v=2'
} = Astro.props;

import Header from '../components/common/Header.astro';
import Footer from '../components/common/Footer.astro';
import FloatingCTA from '../components/common/FloatingCTA.astro';
import '../styles/global.css';
---

<!DOCTYPE html>
<html lang="no">
<head>
  <!-- Prevent theme flash - respects system preference -->
  <script is:inline>
    (function() {
      const savedTheme = localStorage.getItem('theme');
      if (savedTheme) {
        document.documentElement.setAttribute('data-theme', savedTheme);
      } else if (window.matchMedia('(prefers-color-scheme: light)').matches) {
        document.documentElement.setAttribute('data-theme', 'light');
      }
      // Dark theme is default (no attribute needed)
    })();
  </script>
  <ClientRouter />

  <!-- Preload critical fonts to reduce critical path delay -->
  <link rel="preload" as="font" type="font/woff2" crossorigin href="/_astro/inter-latin-400-normal.C38fXH4l.woff2" />
  <link rel="preload" as="font" type="font/woff2" crossorigin href="/_astro/inter-latin-600-normal.LgqL8muc.woff2" />

  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#3b82f6">
  <title>{title}</title>

  <!-- Favicons -->
  <link rel="icon" type="image/png" sizes="32x32" href="/assets/favicon.png">
  <link rel="icon" type="image/svg+xml" href="/assets/logo-icon.svg">
  <link rel="apple-touch-icon" sizes="180x180" href="/assets/favicon.png">
  <link rel="manifest" href="/manifest.json">

  <!-- Meta -->
  <meta name="description" content={description}>
  <meta name="keywords" content="webutvikler, nettside, webapplikasjon, AI, Norge, EFFFEKT">
  <meta name="author" content="EFFFEKT">
  <meta name="robots" content="index, follow, max-snippet:-1, max-image-preview:large">
  <link rel="canonical" href={canonical}>

  <!-- Open Graph -->
  <meta property="og:type" content="website">
  <meta property="og:url" content={canonical}>
  <meta property="og:title" content={title}>
  <meta property="og:description" content={description}>
  <meta property="og:image" content={ogImage}>
  <meta property="og:image:width" content="1200">
  <meta property="og:image:height" content="630">
  <meta property="og:locale" content="no_NO">

  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content={title}>
  <meta name="twitter:description" content={description}>
  <meta name="twitter:image" content={ogImage}>

  <!-- Structured Data -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "Organization",
    "name": "EFFFEKT",
    "url": "https://efffekt.no",
    "logo": "https://efffekt.no/assets/logo.svg",
    "description": "To utviklere som bygger nettsider, webapplikasjoner og AI-løsninger for norske bedrifter.",
    "email": "sander@efffekt.no",
    "telephone": "+47 451 40 089",
    "address": {
      "@type": "PostalAddress",
      "addressCountry": "NO"
    },
    "sameAs": [],
    "founder": [
      {
        "@type": "Person",
        "name": "Sander Martinsen",
        "jobTitle": "Full-Stack Developer"
      },
      {
        "@type": "Person",
        "name": "Daniel Barlag",
        "jobTitle": "Full-Stack Developer"
      }
    ],
    "offers": {
      "@type": "AggregateOffer",
      "priceCurrency": "NOK",
      "lowPrice": "9900",
      "highPrice": "44900",
      "offerCount": "4"
    }
  }
  </script>
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "LocalBusiness",
    "name": "EFFFEKT",
    "image": "https://efffekt.no/assets/og-image.png?v=2",
    "url": "https://efffekt.no",
    "telephone": "+47 451 40 089",
    "email": "sander@efffekt.no",
    "priceRange": "$$",
    "address": {
      "@type": "PostalAddress",
      "addressCountry": "NO"
    },
    "geo": {
      "@type": "GeoCoordinates",
      "latitude": "59.9139",
      "longitude": "10.7522"
    },
    "areaServed": {
      "@type": "Country",
      "name": "Norway"
    },
    "serviceType": ["Web Development", "Web Application Development", "AI Integration"]
  }
  </script>

  <slot name="head" />

  <!-- Microsoft Clarity - deferred to reduce critical path -->
  <script is:inline>
    (function() {
      function loadClarity() {
        (function(c,l,a,r,i,t,y){
            c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
            t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
            y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
        })(window, document, "clarity", "script", "v3kg6mttab");
      }
      if ('requestIdleCallback' in window) {
        requestIdleCallback(loadClarity);
      } else {
        setTimeout(loadClarity, 2000);
      }
    })();
  </script>
</head>
<body class="intro-active">

  <!-- Hero Background Image -->
  <div class="hero-background intro-hidden" id="hero-bg-container" transition:persist>
    <img src="/assets/hero-bg.jpg" alt="" id="hero-bg" loading="eager" fetchpriority="high">
    <div class="hero-overlay" id="hero-overlay"></div>
  </div>


  <script is:inline>
    (function() {
      let scrollHandler = null;

      function initHeroBlur() {
        const heroBg = document.getElementById('hero-bg');
        const overlay = document.getElementById('hero-overlay');

        if (heroBg) {
          // Reset blur on init (viktig for navigasjon)
          heroBg.style.filter = 'blur(0px)';
          heroBg.style.transform = 'scale(1)';
        }

        if (overlay) {
          overlay.style.opacity = '0';
        }

        // Fjern gammel handler hvis den finnes
        if (scrollHandler) {
          window.removeEventListener('scroll', scrollHandler);
        }

        scrollHandler = () => {
          const scrollY = window.scrollY;
          const windowHeight = window.innerHeight;

          // Myk overgang: blur øker gradvis over første 30% av viewport
          const blurEnd = windowHeight * 0.3;
          const progress = Math.min(1, scrollY / blurEnd);

          // Blur fra 0 til 8px, overlay fra 0 til 0.4
          const blurAmount = progress * 8;
          const overlayOpacity = progress * 0.4;

          if (heroBg) {
            heroBg.style.filter = `blur(${blurAmount}px)`;
            heroBg.style.transform = `scale(${1 + blurAmount * 0.01})`;
          }

          if (overlay) {
            overlay.style.opacity = overlayOpacity;
          }
        };

        window.addEventListener('scroll', scrollHandler);

        // Kjør umiddelbart for å sette riktig tilstand basert på scroll-posisjon
        scrollHandler();
      }

      // Kjør ved første lasting
      initHeroBlur();

      // Re-initialiser ved View Transitions navigasjon
      document.addEventListener('astro:page-load', initHeroBlur);
    })();
  </script>

  <!-- Intro Animation Controller -->
  <script is:inline>
    function initIntro() {
      const heroContainer = document.getElementById('hero-bg-container');

      // Skip intro if already seen this session or on navigation
      const hasSeenIntro = sessionStorage.getItem('intro-seen');
      const isNavigation = window.performance && window.performance.getEntriesByType('navigation')[0]?.type === 'back_forward';
      const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

      if (hasSeenIntro || isNavigation || prefersReducedMotion) {
        document.body.classList.remove('intro-active');
        if (heroContainer) heroContainer.classList.remove('intro-hidden');
        return;
      }

      // Start intro animation
      sessionStorage.setItem('intro-seen', 'true');

      if (heroContainer) {
        heroContainer.classList.remove('intro-hidden');
        heroContainer.classList.add('intro-animating');
      }

      setTimeout(() => {
        document.body.classList.remove('intro-active');
        if (heroContainer) {
          heroContainer.classList.remove('intro-animating');
        }
      }, 500);
    }

    initIntro();
    document.addEventListener('astro:page-load', initIntro);
  </script>

  <a href="#main-content" class="skip-link">Hopp til hovedinnhold</a>

  <Header transition:persist />
  <FloatingCTA />

  <main id="main-content" transition:animate="fade">
    <slot />
  </main>

  <Footer transition:persist />

  <slot name="scripts" />
</body>
</html>
